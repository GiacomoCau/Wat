(log 1)

#|
"|#"
(log -1)
"\"|#"
(log -2)
; |# (log -3)
; "|#" (log -4)
; "\n|#" (log -5)
; \n |# (log -6)
|#

(log 2)

#|
(log 9)
; "|#" (log -7)
""
\"
|#

(log 3)

; "\n" (log -8)

; commenti di linea hanno sempre bisogno di un \n per terminare
; a fine file seguito da almeno un carattere e senza il \n da errore
; ora sembra corretto!
; 